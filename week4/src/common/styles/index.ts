import { DefaultTheme } from '@types';

const colors = {
  'gray-0': '#DBDBDB',
  'gray-1': '#717171',
  'gray-2': '#555555',
  'gray-3': '#414141',
  'gray-4': '#363636',
  yellow: '#FFD400',
};

const theme: DefaultTheme = {
  colors,
  border: `border: 4px solid rgba(105, 105, 105, 0.5);`,
  shadow: `box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.25);`,
};

export default theme;
